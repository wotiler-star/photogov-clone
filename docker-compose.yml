services:
  photogov:
    build: .
    image: photogov-clone:latest
    container_name: photogov
    ports:
      - "${PORT:-3000}:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      NODE_ENV: production
    restart: unless-stopped
